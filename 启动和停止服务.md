# LedgerLens 启动和停止指南

## ✨ 智能端口发现机制

前端会**自动检测**后端运行的端口！

工作原理：
1. 后端启动时自动写入端口配置到 `backend-port.json`
2. 前端启动时自动读取配置并更新 `.env.local`
3. 无需手动修改配置文件

---

## 🚀 方法 1：使用脚本（推荐）

### 启动所有服务
```powershell
.\start-all.ps1
```
这会：
- 自动清理残留进程
- 启动后端（新窗口，自动选择可用端口）
- 等待后端写入端口配置
- 启动前端（新窗口，自动连接后端）

### 停止所有服务
```powershell
.\stop-all.ps1
```
这会：
- 停止所有前端 Node.js 进程
- 停止所有后端 Python 进程

---

## 📝 方法 2：手动启动和停止

### 后端

**启动：**
```bash
cd backend
python run_backend.py
```

**停止：**
- 在后端 terminal 按 `Ctrl+C`
- 等待看到 "Finished server process" 消息

### 前端

**启动：**
```bash
cd frontend
npm run dev
```

**停止：**
- 在前端 terminal 按 `Ctrl+C`
- 等待看到 "Gracefully shut down" 消息

---

## ⚠️ 重要提示

### ❌ 不要这样做：
1. ❌ 直接关闭 terminal 窗口
2. ❌ 关闭 Cursor 而不先停止服务
3. ❌ 点击 terminal 的 X 按钮

### ✅ 正确的做法：
1. ✅ 使用 `Ctrl+C` 停止服务
2. ✅ 等待确认消息
3. ✅ 使用 `stop-all.ps1` 清理残留进程

---

## 🔍 检查运行状态

### 查看所有运行中的进程：
```powershell
# 查看前端进程
Get-Process -Name node -ErrorAction SilentlyContinue

# 查看后端进程
Get-Process -Name python -ErrorAction SilentlyContinue | Where-Object {
    $cmd = (Get-CimInstance Win32_Process -Filter "ProcessId = $($_.Id)").CommandLine
    $cmd -like "*uvicorn*" -or $cmd -like "*run_backend*"
}
```

### 查看端口占用：
```powershell
netstat -ano | Select-String ":3000|:8000|:8081|:8082|:8083" | Select-String "LISTENING"
```

---

## 🧹 清理僵尸进程

如果发现有残留的进程或端口被占用：

```powershell
.\stop-all.ps1
```

或者重启电脑（最彻底）。

---

## 💡 最佳实践

1. **每天开始工作时：**
   ```powershell
   .\start-all.ps1
   ```

2. **每天结束工作时：**
   ```powershell
   .\stop-all.ps1
   ```

3. **遇到问题时：**
   - 先运行 `stop-all.ps1`
   - 等待 2-3 秒
   - 再运行 `start-all.ps1`

---

## 📌 快捷操作

在 Cursor 中可以创建快捷任务：

**`.vscode/tasks.json`:**
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start All Services",
      "type": "shell",
      "command": "powershell -File ./start-all.ps1",
      "problemMatcher": []
    },
    {
      "label": "Stop All Services",
      "type": "shell",
      "command": "powershell -File ./stop-all.ps1",
      "problemMatcher": []
    }
  ]
}
```

然后按 `Ctrl+Shift+P` → 输入 "Tasks: Run Task" → 选择相应任务。
